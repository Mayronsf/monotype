# Copie este conteúdo e crie um arquivo .env na raiz do projeto
# IMPORTANTE: Substitua os valores abaixo pelas suas credenciais reais do Supabase

SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_ANON_KEY=sua-chave-anon-public-key-aqui

# Opcional: Service Role Key (apenas se necessário para operações administrativas)
# SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui

PORT=3001
NODE_ENV=development
VITE_API_URL=http://localhost:3001/api

